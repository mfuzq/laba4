version: 2

sphinx:
  configuration: docs/conf.py

python:
  version: 3.8
  install:
    - requirements: requirements.txt

build:
  image: latest
  os: ubuntu-lts-latest
  commands:
    - doxygen Doxyfile
